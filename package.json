{
  "dependencies": {
    "tailwindcss": "^3.4.17"
  },
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./public/style.css --minify",
    "postinstall": "chmod +x node_modules/.bin/*"
  }
}